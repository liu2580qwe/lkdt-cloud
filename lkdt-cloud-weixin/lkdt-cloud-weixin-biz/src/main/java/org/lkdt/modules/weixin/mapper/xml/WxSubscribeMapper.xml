<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lkdt.modules.weixin.mapper.WxSubscribeMapper">
    <select id="getOpenidsByHwId" resultType="java.lang.String">
        select DISTINCT a.openid
        from zc_wx_subscribe a
        where a.subscribe_highway = #{hwId}
    </select>
    <select id="getHwIdsByOpenid" resultType="java.lang.String">
        select DISTINCT a.subscribe_highway
        from zc_wx_subscribe a
        where a.openid = #{openid}
    </select>
    <delete id="removeByOpenIdAndHwId">
        delete from zc_wx_subscribe where openid = #{openid} and subscribe_highway = #{hwId}
    </delete>

    <delete id="removeByOpenId">
        delete from zc_wx_subscribe where openid = #{openid}
    </delete>
    <select id="getOpenidsByWindId" resultType="java.lang.String">
        select DISTINCT b.openid
        from zc_wind a,zc_wx_subscribe b
        where a.hw_id=b.subscribe_highway
          and a.id=#{windId}
    </select>
</mapper>